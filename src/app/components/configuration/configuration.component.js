/**
 * configurationView component
 * @name configurationView
 * @memberof home
 */
(function () {
  'use strict';

  angular
    .module('home')
    .component('configurationView', {
      template: 'CONFIG'
    });
})();
