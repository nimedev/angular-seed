/** constants constant. */
(function() {
  'use strict';

  // application constants
  angular
    .module('angular-seed')
    .constant('constants', {
      // server base url
      REST_URL: '',

      // Debug mode
      DEBUG: true
    });
}());
